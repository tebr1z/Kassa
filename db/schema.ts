import { boolean, date, index, jsonb, numeric, pgEnum, pgTable, text, timestamp, uniqueIndex, uuid } from "drizzle-orm/pg-core";
import { sql } from "drizzle-orm";

export const movementType = pgEnum("movement_type", ["receipt", "sale", "return", "transfer_in", "transfer_out", "adjustment", "write_off"]);
export const branches = pgTable("branches", { id:uuid("id").defaultRandom().primaryKey(), name:text("name").notNull(), address:text("address"), phone:text("phone"), currency:text("currency").default("AZN").notNull(), timezone:text("timezone").default("Asia/Baku").notNull(), isActive:boolean("is_active").default(true).notNull(), createdAt:timestamp("created_at",{withTimezone:true}).defaultNow().notNull() });
export const users = pgTable("users", { id:uuid("id").defaultRandom().primaryKey(), branchId:uuid("branch_id").references(()=>branches.id), fullName:text("full_name").notNull(), role:text("role").notNull(), pinHash:text("pin_hash"), phone:text("phone"), salary:numeric("salary",{precision:14,scale:2}), hiredAt:date("hired_at").defaultNow(), isActive:boolean("is_active").default(true).notNull(), createdAt:timestamp("created_at",{withTimezone:true}).defaultNow().notNull() });
export const warehouses = pgTable("warehouses", { id:uuid("id").defaultRandom().primaryKey(), branchId:uuid("branch_id").references(()=>branches.id).notNull(), name:text("name").notNull(), code:text("code").notNull().unique() });
export const locations = pgTable("warehouse_locations", { id:uuid("id").defaultRandom().primaryKey(), warehouseId:uuid("warehouse_id").references(()=>warehouses.id).notNull(), code:text("code").notNull(), zone:text("zone"), aisle:text("aisle"), shelf:text("shelf"), bin:text("bin") });
export const products = pgTable("products", { id:uuid("id").defaultRandom().primaryKey(), sku:text("sku").notNull().unique(), name:text("name").notNull(), category:text("category"), unit:text("unit").default("ədəd").notNull(), salePrice:numeric("sale_price",{precision:14,scale:2}).notNull(), costPrice:numeric("cost_price",{precision:14,scale:2}).notNull(), minStock:numeric("min_stock",{precision:14,scale:3}).default("0").notNull(), isActive:boolean("is_active").default(true).notNull(), createdAt:timestamp("created_at",{withTimezone:true}).defaultNow().notNull() });
export const barcodes = pgTable("product_barcodes", { id:uuid("id").defaultRandom().primaryKey(), productId:uuid("product_id").references(()=>products.id).notNull(), barcode:text("barcode").notNull().unique(), isPrimary:boolean("is_primary").default(false).notNull() });
export const stockMovements = pgTable("stock_movements", { id:uuid("id").defaultRandom().primaryKey(), productId:uuid("product_id").references(()=>products.id).notNull(), warehouseId:uuid("warehouse_id").references(()=>warehouses.id).notNull(), locationId:uuid("location_id").references(()=>locations.id), type:movementType("type").notNull(), quantity:numeric("quantity",{precision:14,scale:3}).notNull(), unitCost:numeric("unit_cost",{precision:14,scale:2}), referenceType:text("reference_type"), referenceId:uuid("reference_id"), performedBy:uuid("performed_by").references(()=>users.id).notNull(), note:text("note"), createdAt:timestamp("created_at",{withTimezone:true}).defaultNow().notNull() }, table=>[index("stock_product_warehouse_idx").on(table.productId,table.warehouseId),index("stock_created_at_idx").on(table.createdAt)]);
export const shifts = pgTable("cashier_shifts", { id:uuid("id").defaultRandom().primaryKey(), branchId:uuid("branch_id").references(()=>branches.id).notNull(), cashierId:uuid("cashier_id").references(()=>users.id).notNull(), openingCash:numeric("opening_cash",{precision:14,scale:2}).notNull(), expectedCash:numeric("expected_cash",{precision:14,scale:2}), actualCash:numeric("actual_cash",{precision:14,scale:2}), openedAt:timestamp("opened_at",{withTimezone:true}).defaultNow().notNull(), closedAt:timestamp("closed_at",{withTimezone:true}) });
export const customers = pgTable("customers", { id:uuid("id").defaultRandom().primaryKey(), fullName:text("full_name").notNull(), phone:text("phone"), createdAt:timestamp("created_at",{withTimezone:true}).defaultNow().notNull() });
export const salesOrders = pgTable("sales_orders", { id:uuid("id").defaultRandom().primaryKey(), orderNo:text("order_no").notNull().unique(), customerId:uuid("customer_id").references(()=>customers.id).notNull(), status:text("status").default("pending").notNull(), total:numeric("total",{precision:14,scale:2}).notNull(), note:text("note"), createdBy:uuid("created_by").references(()=>users.id).notNull(), createdAt:timestamp("created_at",{withTimezone:true}).defaultNow().notNull(), updatedAt:timestamp("updated_at",{withTimezone:true}).defaultNow().notNull() }, table=>[index("sales_orders_status_idx").on(table.status,table.createdAt)]);
export const sales = pgTable("sales", { id:uuid("id").defaultRandom().primaryKey(), receiptNo:text("receipt_no").notNull().unique(), shiftId:uuid("shift_id").references(()=>shifts.id).notNull(), cashierId:uuid("cashier_id").references(()=>users.id).notNull(), customerId:uuid("customer_id").references(()=>customers.id), salesOrderId:uuid("sales_order_id").references(()=>salesOrders.id), subtotal:numeric("subtotal",{precision:14,scale:2}).notNull(), discount:numeric("discount",{precision:14,scale:2}).default("0").notNull(), total:numeric("total",{precision:14,scale:2}).notNull(), status:text("status").default("completed").notNull(), createdAt:timestamp("created_at",{withTimezone:true}).defaultNow().notNull() }, table=>[uniqueIndex("sales_order_sale_idx").on(table.salesOrderId).where(sql`${table.salesOrderId} is not null`)]);
export const saleItems = pgTable("sale_items", { id:uuid("id").defaultRandom().primaryKey(), saleId:uuid("sale_id").references(()=>sales.id).notNull(), productId:uuid("product_id").references(()=>products.id).notNull(), quantity:numeric("quantity",{precision:14,scale:3}).notNull(), unitPrice:numeric("unit_price",{precision:14,scale:2}).notNull(), costSnapshot:numeric("cost_snapshot",{precision:14,scale:2}).notNull(), lineTotal:numeric("line_total",{precision:14,scale:2}).notNull() });
export const payments = pgTable("payments", { id:uuid("id").defaultRandom().primaryKey(), saleId:uuid("sale_id").references(()=>sales.id).notNull(), method:text("method").notNull(), amount:numeric("amount",{precision:14,scale:2}).notNull(), reference:text("reference"), createdAt:timestamp("created_at",{withTimezone:true}).defaultNow().notNull() });
export const auditLogs = pgTable("audit_logs", { id:uuid("id").defaultRandom().primaryKey(), userId:uuid("user_id").references(()=>users.id), action:text("action").notNull(), entityType:text("entity_type").notNull(), entityId:uuid("entity_id"), beforeJson:text("before_json"), afterJson:text("after_json"), ipAddress:text("ip_address"), createdAt:timestamp("created_at",{withTimezone:true}).defaultNow().notNull() });
export const suppliers = pgTable("suppliers", { id:uuid("id").defaultRandom().primaryKey(), name:text("name").notNull(), phone:text("phone"), email:text("email"), address:text("address"), isActive:boolean("is_active").default(true).notNull(), createdAt:timestamp("created_at",{withTimezone:true}).defaultNow().notNull() });
export const purchaseOrders = pgTable("purchase_orders", { id:uuid("id").defaultRandom().primaryKey(), orderNo:text("order_no").notNull().unique(), supplierId:uuid("supplier_id").references(()=>suppliers.id).notNull(), status:text("status").default("ordered").notNull(), expectedDate:date("expected_date"), total:numeric("total",{precision:14,scale:2}).notNull(), note:text("note"), createdBy:uuid("created_by").references(()=>users.id).notNull(), createdAt:timestamp("created_at",{withTimezone:true}).defaultNow().notNull(), receivedAt:timestamp("received_at",{withTimezone:true}) }, table=>[index("purchase_orders_supplier_idx").on(table.supplierId)]);
export const purchaseOrderItems = pgTable("purchase_order_items", { id:uuid("id").defaultRandom().primaryKey(), purchaseOrderId:uuid("purchase_order_id").references(()=>purchaseOrders.id).notNull(), productId:uuid("product_id").references(()=>products.id).notNull(), quantity:numeric("quantity",{precision:14,scale:3}).notNull(), unitCost:numeric("unit_cost",{precision:14,scale:2}).notNull(), lineTotal:numeric("line_total",{precision:14,scale:2}).notNull() }, table=>[index("purchase_items_order_idx").on(table.purchaseOrderId)]);
export const financeTransactions = pgTable("finance_transactions", { id:uuid("id").defaultRandom().primaryKey(), type:text("type").notNull(), category:text("category").notNull(), description:text("description").notNull(), account:text("account").default("cash").notNull(), amount:numeric("amount",{precision:14,scale:2}).notNull(), supplierId:uuid("supplier_id").references(()=>suppliers.id), purchaseOrderId:uuid("purchase_order_id").references(()=>purchaseOrders.id), createdBy:uuid("created_by").references(()=>users.id).notNull(), occurredAt:timestamp("occurred_at",{withTimezone:true}).defaultNow().notNull(), createdAt:timestamp("created_at",{withTimezone:true}).defaultNow().notNull() }, table=>[index("finance_transactions_occurred_idx").on(table.occurredAt)]);
export const supplierPayments = pgTable("supplier_payments", { id:uuid("id").defaultRandom().primaryKey(), supplierId:uuid("supplier_id").references(()=>suppliers.id).notNull(), purchaseOrderId:uuid("purchase_order_id").references(()=>purchaseOrders.id), amount:numeric("amount",{precision:14,scale:2}).notNull(), account:text("account").default("bank").notNull(), note:text("note"), createdBy:uuid("created_by").references(()=>users.id).notNull(), paidAt:timestamp("paid_at",{withTimezone:true}).defaultNow().notNull() }, table=>[index("supplier_payments_supplier_idx").on(table.supplierId,table.paidAt)]);
export const employeeAttendance = pgTable("employee_attendance", { id:uuid("id").defaultRandom().primaryKey(), userId:uuid("user_id").references(()=>users.id).notNull(), clockIn:timestamp("clock_in",{withTimezone:true}).defaultNow().notNull(), clockOut:timestamp("clock_out",{withTimezone:true}), note:text("note"), createdBy:uuid("created_by").references(()=>users.id).notNull() }, table=>[index("employee_attendance_user_idx").on(table.userId,table.clockIn)]);
export const salesOrderItems = pgTable("sales_order_items", { id:uuid("id").defaultRandom().primaryKey(), salesOrderId:uuid("sales_order_id").references(()=>salesOrders.id).notNull(), productId:uuid("product_id").references(()=>products.id).notNull(), quantity:numeric("quantity",{precision:14,scale:3}).notNull(), unitPrice:numeric("unit_price",{precision:14,scale:2}).notNull(), lineTotal:numeric("line_total",{precision:14,scale:2}).notNull() }, table=>[index("sales_order_items_order_idx").on(table.salesOrderId)]);
export const authSessions = pgTable("auth_sessions", { id:uuid("id").defaultRandom().primaryKey(), userId:uuid("user_id").references(()=>users.id,{onDelete:"cascade"}).notNull(), tokenHash:text("token_hash").notNull().unique(), expiresAt:timestamp("expires_at",{withTimezone:true}).notNull(), createdAt:timestamp("created_at",{withTimezone:true}).defaultNow().notNull() }, table=>[index("auth_sessions_user_idx").on(table.userId,table.expiresAt)]);
export const heldReceipts = pgTable("held_receipts", { id:uuid("id").defaultRandom().primaryKey(), cashierId:uuid("cashier_id").references(()=>users.id).notNull(), customerId:uuid("customer_id").references(()=>customers.id), itemsJson:jsonb("items_json").notNull(), discount:numeric("discount",{precision:14,scale:2}).default("0").notNull(), note:text("note"), createdAt:timestamp("created_at",{withTimezone:true}).defaultNow().notNull() }, table=>[index("held_receipts_cashier_idx").on(table.cashierId,table.createdAt)]);
export const productPriceHistory = pgTable("product_price_history", { id:uuid("id").defaultRandom().primaryKey(), productId:uuid("product_id").references(()=>products.id,{onDelete:"cascade"}).notNull(), oldSalePrice:numeric("old_sale_price",{precision:14,scale:2}).notNull(), newSalePrice:numeric("new_sale_price",{precision:14,scale:2}).notNull(), oldCostPrice:numeric("old_cost_price",{precision:14,scale:2}).notNull(), newCostPrice:numeric("new_cost_price",{precision:14,scale:2}).notNull(), changedBy:uuid("changed_by").references(()=>users.id).notNull(), changedAt:timestamp("changed_at",{withTimezone:true}).defaultNow().notNull() }, table=>[index("product_price_history_product_idx").on(table.productId,table.changedAt)]);
export const inventoryCounts = pgTable("inventory_counts", { id:uuid("id").defaultRandom().primaryKey(), countNo:text("count_no").notNull().unique(), warehouseId:uuid("warehouse_id").references(()=>warehouses.id).notNull(), status:text("status").default("completed").notNull(), note:text("note"), createdBy:uuid("created_by").references(()=>users.id).notNull(), createdAt:timestamp("created_at",{withTimezone:true}).defaultNow().notNull(), completedAt:timestamp("completed_at",{withTimezone:true}).defaultNow().notNull() }, table=>[index("inventory_counts_created_idx").on(table.createdAt)]);
export const inventoryCountItems = pgTable("inventory_count_items", { id:uuid("id").defaultRandom().primaryKey(), inventoryCountId:uuid("inventory_count_id").references(()=>inventoryCounts.id,{onDelete:"cascade"}).notNull(), productId:uuid("product_id").references(()=>products.id).notNull(), systemQuantity:numeric("system_quantity",{precision:14,scale:3}).notNull(), actualQuantity:numeric("actual_quantity",{precision:14,scale:3}).notNull(), difference:numeric("difference",{precision:14,scale:3}).notNull() });
export const cashRegisters = pgTable("cash_registers", { id:uuid("id").defaultRandom().primaryKey(), branchId:uuid("branch_id").references(()=>branches.id).notNull(), name:text("name").notNull(), code:text("code").notNull().unique(), isActive:boolean("is_active").default(true).notNull(), createdAt:timestamp("created_at",{withTimezone:true}).defaultNow().notNull() });
export const systemSettings = pgTable("system_settings", { key:text("key").primaryKey(), value:text("value").notNull(), updatedBy:uuid("updated_by").references(()=>users.id), updatedAt:timestamp("updated_at",{withTimezone:true}).defaultNow().notNull() });
export const productCategories = pgTable("product_categories", { id:uuid("id").defaultRandom().primaryKey(), name:text("name").notNull().unique(), isActive:boolean("is_active").default(true).notNull() });
export const systemErrors = pgTable("system_errors", { id:uuid("id").defaultRandom().primaryKey(), source:text("source").notNull(), message:text("message").notNull(), detail:text("detail"), createdAt:timestamp("created_at",{withTimezone:true}).defaultNow().notNull() }, table=>[index("system_errors_created_idx").on(table.createdAt)]);
export const systemBackups = pgTable("system_backups", { id:uuid("id").defaultRandom().primaryKey(), label:text("label").notNull(), payload:jsonb("payload").notNull(), createdBy:uuid("created_by").references(()=>users.id), createdAt:timestamp("created_at",{withTimezone:true}).defaultNow().notNull() });
